(ns act8.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(defn generate-json-from-hash [h]
  "j")

(defn generate-hash-from-json [j]
  "h")